---
- name: print message
  hosts: localhost
  tasks: 
    - ansible.builtin.debug:
        msg: "this is test one"
- name: nginx reverse proxy setup for nodejs
  hosts: localhost
  become: yes
  roles: 
    - "{{ROLE}}"
